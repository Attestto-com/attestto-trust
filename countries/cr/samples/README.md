# samples

Signed PDFs we extracted intermediates from, kept here for reproducibility.

**Only commit a sample if redistribution is OK** — i.e. it's a public document, a synthetic test doc, or you have permission from the signer. Personal or confidential signed documents must NOT be committed.

When in doubt, just leave a note in the commit message describing where the cert came from instead of committing the file.
